{
  "name": "beauty-studio-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env v2-test",
    "test": "node --test \"test/**/*.test.js\""
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
